gnubiffdir = $(pkgdatadir)
dist_gnubiff_DATA = \
		envelopes.png			\
		gnubiff.png				\
		tux-big.png				\
		tux-awake.png			\
		tux-jump(64x64).png		\
		tux-sleep(64x64).png	\
		tux-sleep.png

if USE_GNOME
gnomedir = $(datadir)/pixmaps
dist_gnome_DATA = \
		gnubiff.png
endif
